# Private Endpoints Module

Creates Private Endpoints for Azure services and attaches optional Private DNS zone groups.

Definitions are passed as a map so the root module can keep the rollout explicit while the
resource implementation stays reusable.

## Navigation

- Up: [Terraform Design](../../../docs/terraform-design.md)
