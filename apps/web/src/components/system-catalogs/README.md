# System Catalog Components

Shared React components for system-catalog selection live here. Feature components may import
from this folder, but these components must stay independent from feature component internals
and receive translated display strings through props.
